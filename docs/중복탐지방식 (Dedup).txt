중복탐지방식 (Dedup)
A. 파일 기반 — MD5/SHA, 파일크기 매칭
B. 픽셀 기반 — MSE/PSNR, 컬러 히스토그램
C. 지각 해시 — aHash / dHash / pHash / wHash
D. 학습 모델 기반 — CLIP·DINOv2 embedding, SSCD, SIFT/ORB, (+FAISS는 가속 도구)

<우리 프로젝트의 요구사항으로 거르기>
주적: Stanford↔Tsinghua↔Oxford 간 "인터넷에서 퍼지며 변형된 같은 사진" + "같은 개 연속 컷"
규모: 9.8만 장 → 쌍별 비교(MSE, SIFT 전수)는 탈락
일정: 8/15 하루 → 추가 모델 도입(SSCD)은 부담
함정: 강아지끼리는 원래 서로 닮음 → 히스토그램 탈락, embedding은 threshold 신중
threshold를 높게 시작 (0.92~0.96) — 일반 데이터라면 0.85도 쓰지만 우리는 안 됨
품종별 분포 확인 — 사모예드처럼 개체 간 유사도가 원래 높은 품종은 더 엄격한 값 적용 (쌩눈 검수 필)
거대 그룹 경보 — 그룹 하나에 수십 장 이상 묶이면 threshold가 뚫린 신호이니 재검토

비대칭: 놓침(누수) >> 과다 묶음(낭비) → 단계별로 촘촘하게

<추천 3종 조합>
선택	담당하는 중복	선택 이유 (깔때기 스타일로 췤)
① MD5: 사진 종이가 같은가 (파일)
② pHash: 구도가 같은가 (다른 hash들 대비 pHash가 밝기 변화 등에 제일 영향 덜받음)
③ DINOv2 embedding : 같은 댕댕이의 같은 모먼트인가 (구도가 아니라 사진 내용)
