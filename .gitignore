# ── 대용량 데이터 (폴더 단위로 제외) ───────────────────────────
# 확장자가 아니라 폴더로 막는 이유: Stanford annotation 파일은
# 확장자가 없어서 *.xml / *.txt 같은 규칙에 걸리지 않음

# main 브랜치 정책 (data/ 아래 경로)
data/raw/
data/labeled/
data/processed/
data/release/
data/raw.zip
data/labeled.zip
data/processed.zip
doi_10_5061_dryad_g4f4qrfr0__v20220505/GeneticData.zip

# hch 로컬 경로 (저장소 루트에 바로 있는 경우)
dog_breed_dataset/
processed/

# ── 딥러닝 산출물 ──────────────────────────────────────────
*.pt
*.pth
*.onnx
*.npy
*.npz
*.pkl
runs/
wandb/
checkpoints/
lightning_logs/

# ── 파이썬 ────────────────────────────────────────────────
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
.env

# ── 기타 ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
data/BM_dog_pho/
runs/

# 개인 사진 (public 저장소 유출 방지 — data/BM_dog_pho와 동일 취지)
my_photos/
